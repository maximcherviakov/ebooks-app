name: CI/CD Pipeline

on:
  push:
    branches:
      - main
      - develop
      - release/*
      - feature/*
      - hotfix/*
  pull_request:
    branches:
      - main
      - develop
      - release/*
      - feature/*
      - hotfix/*

jobs:
  test_server:
    name: Run Server Tests
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: server
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: 18

      - name: Install dependencies
        run: npm install

      - name: Run tests with Jest
        run: npm test -- --ci --reporters=default --reporters=jest-junit --reporters=jest-html-reporter
        continue-on-error: true

      - name: Upload Jest test results
        uses: actions/upload-artifact@v4
        with:
          name: jest-results
          path: server/junit.xml

      - name: Upload HTML Test Report
        uses: actions/upload-artifact@v4
        with:
          name: test-report-html
          path: server/test-report.html

  e2e-tests:
    name: Run UI Tests
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Create .env file
        run: |
          echo "BACKEND_HOST=${{ secrets.BACKEND_HOST }}" >> .env
          echo "BACKEND_PORT=${{ secrets.BACKEND_PORT }}" >> .env
          echo "BACKEND_UPLOADED_BOOKS_PATH=${{ secrets.BACKEND_UPLOADED_BOOKS_PATH }}" >> .env
          echo "BACKEND_UPLOADED_THUMBNAILS_PATH=${{ secrets.BACKEND_UPLOADED_THUMBNAILS_PATH }}" >> .env
          echo "BACKEND_JWT_SECRET_KEY=${{ secrets.BACKEND_JWT_SECRET_KEY }}" >> .env
          echo "BACKEND_JWT_EXPIRES_IN=${{ secrets.BACKEND_JWT_EXPIRES_IN }}" >> .env
          echo "BACKEND_GOOGLE_CLIENT_ID=${{ secrets.BACKEND_GOOGLE_CLIENT_ID }}" >> .env
          echo "BACKEND_GOOGLE_CLIENT_SECRET=${{ secrets.BACKEND_GOOGLE_CLIENT_SECRET }}" >> .env
          echo "FRONTEND_HOST=${{ secrets.FRONTEND_HOST }}" >> .env
          echo "FRONTEND_PORT=${{ secrets.FRONTEND_PORT }}" >> .env
          echo "MONGO_HOST=${{ secrets.MONGO_HOST }}" >> .env
          echo "MONGO_PORT=${{ secrets.MONGO_PORT }}" >> .env
          echo "MONGO_DB=${{ secrets.MONGO_DB }}" >> .env
          echo "MONGO_USER=${{ secrets.MONGO_USER }}" >> .env
          echo "MONGO_PASS=${{ secrets.MONGO_PASS }}" >> .env

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v1

      - name: Install Docker Compose
        run: |
          sudo apt-get update
          sudo apt-get install -y docker-compose

      - name: Build and start containers
        run: docker-compose -f docker-compose.ci.yaml up -d

      - name: Wait for services to be ready
        run: |
          # Function to check if a URL is available
          check_url() {
            curl --silent --fail --max-time 5 "$1" > /dev/null 2>&1
            return $?
          }

          # Function to check if a container is running
          check_container() {
            docker ps --filter "name=$1" --filter "status=running" --quiet | grep -q .
            return $?
          }

          # Function to wait for a service with timeout
          wait_for_service() {
            local service_name="$1"
            local check_command="$2"
            local max_attempts=30
            local wait_seconds=2
            
            echo "Waiting for $service_name to be ready..."
            for i in $(seq 1 $max_attempts); do
              if eval "$check_command"; then
                echo "✅ $service_name is ready!"
                return 0
              fi
              
              echo "Attempt $i/$max_attempts: $service_name not ready yet, waiting ${wait_seconds}s..."
              sleep $wait_seconds
            done
            
            echo "❌ Error: $service_name not ready after $(($max_attempts * $wait_seconds)) seconds"
            return 1
          }

          # First check if all containers are running
          wait_for_service "proxy container" "check_container ebooks-app-proxy" || exit 1
          wait_for_service "backend container" "check_container ebooks-app-backend" || exit 1
          wait_for_service "client container" "check_container ebooks-app-client" || exit 1
          wait_for_service "mongo container" "check_container ebooks-app-mongo" || exit 1

          # Then verify services are responding correctly
          wait_for_service "client service" "check_url http://localhost" || exit 1
          wait_for_service "backend API" "check_url http://localhost/api/books/genres" || exit 1

          echo "🚀 All services are ready and responding!"

      - name: Run UI Tests
        run: |
          docker-compose -f docker-compose.ci.yaml up --exit-code-from ui-tests ui-tests
          test_exit_code=$?
          echo "UI tests completed with exit code: $test_exit_code"
          if [ $test_exit_code -eq 0 ]; then
            echo "tests_ran=true" >> $GITHUB_ENV
          else
            echo "tests_ran=false" >> $GITHUB_ENV
          fi

      - name: Shut down containers
        run: docker-compose -f docker-compose.ci.yaml down

      - uses: actions/upload-artifact@v4
        if: env.tests_ran == 'true' && !cancelled()
        with:
          name: playwright-report
          path: playwright-report/
          retention-days: 30

  lint:
    name: Run ESLint
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: 18

      - name: Install dependencies (server)
        run: npm install
        working-directory: server

      - name: Install dependencies (client)
        run: npm install
        working-directory: client

      - name: Run ESLint and generate HTML report (server)
        run: npm run lint:html
        working-directory: server

      - name: Run ESLint and generate HTML report (client)
        run: npm run lint:html
        working-directory: client

      - name: Upload ESLint HTML Report (server)
        uses: actions/upload-artifact@v4
        with:
          name: eslint-report-server
          path: server/eslint-report.html

      - name: Upload ESLint HTML Report (client)
        uses: actions/upload-artifact@v4
        with:
          name: eslint-report-client
          path: client/eslint-report.html

  analyze:
    name: Analyze code with CodeQL
    runs-on: "ubuntu-latest"
    permissions:
      security-events: write
      packages: read
      actions: read
      contents: read

    strategy:
      fail-fast: false
      matrix:
        include:
          - language: javascript-typescript
            build-mode: none
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Initialize CodeQL
        uses: github/codeql-action/init@v3
        with:
          languages: ${{ matrix.language }}
          build-mode: ${{ matrix.build-mode }}

      - name: Perform CodeQL Analysis
        uses: github/codeql-action/analyze@v3
        with:
          category: "/language:${{matrix.language}}"
